name: Deploy site

on:
  push:
    branches:
      - main
    paths:
      - site/**
      - .github/workflows/pages.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    name: Sync site to gh-pages
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Copy site to gh-pages
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin gh-pages
          git worktree add /tmp/aerial-gh-pages origin/gh-pages
          rsync -a --delete --exclude .git site/ /tmp/aerial-gh-pages/
          cd /tmp/aerial-gh-pages
          if git diff --quiet; then
            echo "gh-pages already matches site/"
            exit 0
          fi
          git add -A
          git commit -m "Deploy site"
          git push origin HEAD:gh-pages
